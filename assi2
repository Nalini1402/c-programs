#include <stdio.h>
void main() {
  int r,sp,c,n;
  printf("enter the value:");
  scanf("%d",&n);
  for(r=1;r<=n;r++)
  {
      for(sp=1;sp<n-r;sp++)
      {
          printf(" ");
      }
      for(c=1;c<=(2*r-1);c++)
      {
          printf("*");
      }
      printf("\n");
  }

    return 0;
}
